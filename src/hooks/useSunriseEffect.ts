export const useSunriseEffect = (isDark: boolean) => {
  // We removed direct DOM manipulations and GSAP that caused freezing on mobile.
  // The theme transitions are now handled entirely by CSS variables and standard liquid transitions.
};
